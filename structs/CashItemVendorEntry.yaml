# yaml-language-server: $schema=../otherland.packet.schema.yaml

structures:
  CashItemVendorEntry:
    fields:
      - name: version
        type: u32
      - name: vendorID
        type: cstring
      - name: vendorName
        type: cstring
      - name: skuList
        type: cstring
      - name: bundleList
        type: cstring