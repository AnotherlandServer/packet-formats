# yaml-language-server: $schema=../otherland.packet.schema.yaml

packets:
  CPktChannelChat:
    id: 0x3e
    subId: 0x0
    inherit: CPkt
    fields:
      - type: cstring
        name: channel
      - type: cstring
        name: message