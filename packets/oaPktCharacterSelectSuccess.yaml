# yaml-language-server: $schema=../otherland.packet.schema.yaml

id: 0x7e
subId: 0x09
inherit: CPkt
fields:
  - type: u32
  - type: u32
  - name: worldIp
    type: u32
  - name: worldPort
    type: u16
  - name: magicBytes
    type: { name: array, len: 16, type: u8 }