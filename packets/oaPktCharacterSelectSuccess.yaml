# yaml-language-server: $schema=../otherland.packet.schema.yaml

id: 0x7e
subId: 0x09
inherit: CPkt
fields:
  - type: u32
  - type: u32
  - name: worldIp # i guess
    type: u32
  - name: worldPort # i guess
    type: u16
  - name: magicBytes
    type: { name: array, len: 10, type: u8 }