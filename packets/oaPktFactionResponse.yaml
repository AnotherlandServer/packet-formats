# yaml-language-server: $schema=../otherland.packet.schema.yaml

packets:
  oaPktFactionResponse:
    id: 0x8c
    subId: 0x44
    inherit: CPkt
    fields:
      - type: u64
      - type: u8
      - type: nativeparam