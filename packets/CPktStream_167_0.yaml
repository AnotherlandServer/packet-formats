# yaml-language-server: $schema=../otherland.packet.schema.yaml

# this packet is used for transferring a new character as a result of the
# oaPktCharacterCreate request.

packets:
  CPktStream_167_0:
    id: 0xa7
    subId: 0x0
    inherit: CPkt
    fields:
      - name: friendList
        type: :oaFriendList