# yaml-language-server: $schema=../otherland.packet.schema.yaml

packets:
  oaPktCharacterDeleteSuccess:
    id: 0x7e
    subId: 0x07
    inherit: CPkt
    fields:
      - name: characterId
        type: u32