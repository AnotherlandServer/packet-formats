# yaml-language-server: $schema=../otherland.packet.schema.yaml

packets:
  oaPktRequestSelectWorld:
    id: 0x7e
    subId: 0x0b
    inherit: CPkt
    fields:
      - type: u16
        name: worldId
      - type: u16
      - type: uuid