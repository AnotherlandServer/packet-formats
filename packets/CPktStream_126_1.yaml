# yaml-language-server: $schema=../otherland.packet.schema.yaml

# this packet is used for transferring a character list to the client

packets:
  CPktStream_126_1:
    id: 0x7e
    subId: 0x01
    inherit: CPkt
    fields:
      - name: list
        type: :oaCharacterList
  