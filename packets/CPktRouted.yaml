# yaml-language-server: $schema=../otherland.packet.schema.yaml

packets:
  CPktRouted:
    id: 0x77
    subId: 0x0
    inherit: CPkt
    fields:
      - type: u8
      - type: u8
      - type: u64
      - type: { name: array, len: _remainder, type: u8 }