# yaml-language-server: $schema=../otherland.packet.schema.yaml

id: 0x7e
subId: 0x00
inherit: CPkt
fields:
  # some kind of struct / session uuid
  - type: u32
  - type: u16
  - type: u16
  - type: u8
  - type: u8
  - type: u8
  - type: u8
  - type: u8
  - type: u8
  - type: u8
  - type: u8
  # 10 bytes
  - type: { name: array, len: 10, type: u8 }